# Dependencies for the AI Second Brain ingestion pipeline
# (scripts/ingest_audio.py, scripts/ingest_docs.py).
#
# Install into an isolated venv (this Debian system blocks system-wide pip
# installs — PEP 668):
#   uv venv .venv-second-brain
#   uv pip install -r requirements-second-brain.txt --python .venv-second-brain

faster-whisper>=1.0.0
pypdf>=4.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
